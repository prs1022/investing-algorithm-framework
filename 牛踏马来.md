1. 山寨策略逻辑：
涨幅榜筛选 → RSI/EMA信号确认 → 开仓

特点：
选择24h涨幅榜前N名（默认前5名）
结合RSI+EMA技术指标进行二次确认
支持多重信号过滤（时间过滤、BTC趋势过滤、非农数据过滤）
双重确认机制提高开仓质量

开仓条件：
涨幅榜前5名 + 涨幅≥5% + 成交量达标
RSI < 35（超卖）+ EMA短期 > EMA长期（金叉）
通过时间和事件过滤器

平仓条件：
止损：亏损≥3%
止盈：盈利≥30%
移动止盈：从最高价回落≥10%
策略信号：出现CLOSE_LONG或SHORT信号
无波动平仓：持仓超过1小时且涨幅变化<2%

ps:市场扫描
扫描24h涨幅榜
筛选负费率合约（费率<-1%）
组合筛选（涨幅榜+负费率）



2. 主流币开仓逻辑，使用技术指标

策略逻辑：
单向交易模式（不同时持多空）
基于RSI超买超卖和EMA金叉死叉

具体信号规则：
做多：RSI < 35 + EMA金叉
做空：RSI > 65 + EMA死叉
平多：RSI > 65 或 收到SHORT/CLOSE_LONG信号
平空：RSI < 35 或 收到LONG/CLOSE_SHORT信号

风控特点：
止损止盈机制
移动止盈跟踪
逐仓模式（独立风险）

信号过滤器：
时间过滤：北京时间21:00-23:00不开单
非农过滤：每月第一个星期五20:00-22:00不开单
BTC趋势过滤：信号需与BTC趋势一致


策略参数配置：
# ===================== API 配置 =====================
API_KEY = bconfig.api_key
API_SECRET = bconfig.api_secret
API_HOST = bconfig.api_host
SETTLE_CURRENCY = "usdt"

# ===================== 策略参数 =====================
LEVERAGE = 10  # 杠杆倍数
TOP_N_GAINERS = 20  # 筛选涨幅榜前N名
TOP_N_VOLUME = 8  # 从涨幅榜中选成交量前N名
MIN_CHANGE = 5.0  # 最小涨幅（%）
MAX_CHANGE = 100.0  # 最大涨幅（%）
VOLUME_THRESHOLD = 50000  # 成交量阈值
STOP_LOSS_RATE = 0.03  # 止损比例（3%）
TAKE_PROFIT_RATE = 0.3  # 止盈比例（30%）
TRAILING_STOP_RATE = 0.1  # 移动止盈回落比例（10%）
POLL_INTERVAL = 3 * 60  # 轮询间隔（秒）
MAX_POSITION_RATIO = 0.1  # 单币种最大仓位占比
MAX_POSITIONS = 3  # 最多同时持有的标的数量
NO_MOVEMENT_TIMEOUT = 60 * 60  # 无波动超时时间（秒）
NO_MOVEMENT_THRESHOLD = 0.02  # 无波动阈值

candle_interval: str = "1h"  # K线间隔 (10s,1m,5m,15m,30m,1h,4h,8h,1d)
limit: int = 300  # 最近多少根k线
rsi_period: int = 14
rsi_oversold: int = 35
rsi_overbought: int = 65
ema_short: int = 12
ema_long: int = 26

# ===================== 资金费率筛选参数 =====================
FUNDING_RATE_THRESHOLD = -0.005  # 资金费率阈值（-0.5%）
FUNDING_INTERVAL_HOURS = 1  # 资金费率更新频率（1小时）

最后：目的是想设计一个既能匹配热点的追涨策略，也能在趋势平稳或震荡下的稳健的技术指标策略, 后续需要根据市场环境智能选择合适的策略运行。
