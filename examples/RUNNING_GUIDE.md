# 🚀 实盘交易运行指南

## 运行方式

### 方法 1：直接运行（推荐）

```bash
python examples/gateio_live_trading_with_risk_control.py
```

**特点**：
- 持续运行约 11.5 天（999999 次迭代）
- 每次迭代间隔 1 秒
- 按 `Ctrl+C` 可随时停止

### 方法 2：后台运行

```bash
# 使用 nohup 后台运行
nohup python examples/gateio_live_trading_with_risk_control.py > trading.log 2>&1 &

# 查看日志
tail -f trading.log

# 停止运行
ps aux | grep gateio_live_trading
kill <PID>
```

### 方法 3：使用 screen（推荐用于服务器）

```bash
# 创建新 screen 会话
screen -S trading_bot

# 运行机器人
python examples/gateio_live_trading_with_risk_control.py

# 断开会话（机器人继续运行）
# 按 Ctrl+A 然后按 D

# 重新连接
screen -r trading_bot

# 停止机器人
# 在 screen 中按 Ctrl+C
```

### 方法 4：使用 systemd（Linux 服务）

创建服务文件 `/etc/systemd/system/trading-bot.service`：

```ini
[Unit]
Description=Gate.io Trading Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/investing-algorithm-framework
ExecStart=/usr/bin/python3 examples/gateio_live_trading_with_risk_control.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启动服务：
```bash
sudo systemctl daemon-reload
sudo systemctl start trading-bot
sudo systemctl enable trading-bot  # 开机自启
sudo systemctl status trading-bot  # 查看状态
sudo journalctl -u trading-bot -f  # 查看日志
```

## ⏰ 运行周期

### 策略执行频率
- **时间单位**: 小时（`TimeUnit.HOUR`）
- **间隔**: 2 小时（`interval=2`）
- **实际运行**: 每 2 小时检查一次信号并执行交易

### 事件循环
- **检查频率**: 每秒检查一次是否有策略需要运行
- **数据更新**: 每次策略运行时从交易所获取最新数据
- **风险控制**: 每次策略运行时检查止损/止盈

### 时间线示例
```
00:00 - 策略运行，检查信号，执行交易
00:01 - 00:59 - 等待（每秒检查一次）
01:00 - 01:59 - 等待
02:00 - 策略运行，检查信号，执行交易
04:00 - 策略运行
06:00 - 策略运行
...
```

## 📊 监控运行状态

### 查看日志

**实时日志**：
```bash
# 如果直接运行
# 日志会直接输出到终端

# 如果后台运行
tail -f trading.log

# 查看最近 100 行
tail -100 trading.log

# 搜索特定内容
grep "BUY SIGNAL" trading.log
grep "STOP LOSS" trading.log
```

### 关键日志标识

```
💾 Saved ... rows to ...          # 数据已保存
💼 Positions PnL: ...              # 持仓盈亏
📊 Portfolio Status: ...           # 资产状态
🟢 BUY SIGNAL: ...                 # 买入信号
🔴 SELL SIGNAL: ...                # 卖出信号
💰 FIBONACCI PROFIT TAKING!        # 止盈触发
🛑 STOP LOSS TRIGGERED!            # 止损触发
⚠️  WARNING: Approaching stop-loss # 接近止损线
```

### 检查运行状态

```bash
# 检查进程是否运行
ps aux | grep gateio_live_trading

# 检查最后一次日志时间
tail -1 trading.log

# 检查数据文件更新时间
ls -lt examples/live_trading_data/ | head -5
```

## 🛑 停止机器人

### 正常停止

```bash
# 如果在前台运行
按 Ctrl+C

# 如果在后台运行
kill <PID>

# 如果使用 systemd
sudo systemctl stop trading-bot
```

### 紧急停止

```bash
# 强制终止
kill -9 <PID>

# 注意：强制终止可能导致：
# - 未完成的订单仍然存在
# - 数据未保存
# - 需要手动检查交易所的持仓
```

## ⚠️ 重要注意事项

### 1. 网络连接
- 需要稳定的网络连接
- 建议使用有线网络或稳定的 WiFi
- 服务器运行更可靠

### 2. API 限制
- Gate.io 有 API 调用频率限制
- 框架会自动处理限流
- 如果频繁报错，可能需要降低检查频率

### 3. 资金安全
- 定期检查持仓和余额
- 监控止损是否正常工作
- 保持足够的 USDT 余额用于交易

### 4. 数据备份
- 定期备份数据库文件
- 保存重要的日志文件
- 记录所有交易记录

### 5. 异常处理
- 机器人会自动处理大部分错误
- 严重错误会导致程序退出
- 建议使用 systemd 或 supervisor 自动重启

## 🔧 故障排查

### 问题：机器人立即退出

**可能原因**：
1. API 密钥配置错误
2. 网络连接问题
3. 交易对不支持

**解决方法**：
```bash
# 检查 .env 文件
cat .env

# 测试连接
python examples/test_gateio_connection.py

# 查看完整错误
python examples/gateio_live_trading_with_risk_control.py 2>&1 | tee error.log
```

### 问题：没有执行交易

**可能原因**：
1. 没有触发买入/卖出信号
2. 余额不足
3. 交易对价格不符合条件

**解决方法**：
- 查看日志中的信号统计
- 检查 Gate.io 账户余额
- 调整策略参数（RSI 阈值、EMA 周期等）

### 问题：数据没有保存

**可能原因**：
1. 目录权限问题
2. 磁盘空间不足

**解决方法**：
```bash
# 检查目录权限
ls -la examples/live_trading_data/

# 检查磁盘空间
df -h

# 手动创建目录
mkdir -p examples/live_trading_data
chmod 755 examples/live_trading_data
```

## 📈 性能优化

### 减少资源占用

```python
# 修改检查间隔（从 2 小时改为 4 小时）
time_unit = TimeUnit.HOUR
interval = 4

# 减少数据窗口（从 200 改为 100）
window_size=100
```

### 提高响应速度

```python
# 增加检查频率（从 2 小时改为 30 分钟）
time_unit = TimeUnit.MINUTE
interval = 30
```

## 📞 获取帮助

如果遇到问题：
1. 查看日志文件
2. 检查 Gate.io 账户状态
3. 阅读框架文档
4. 在 GitHub 提 Issue

---

**提示**: 实盘交易有风险，请谨慎操作！建议先用小资金测试。
